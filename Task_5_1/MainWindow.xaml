<Window x:Class="Task_5_1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="InteractiveButtonStyle" TargetType="Button"
           BasedOn="{StaticResource BaseButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource HoverColor}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="FontWeight" Value="Bold"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="{StaticResource DisabledColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Margin="20" Padding="10">
        <StackPanel>
            <TextBlock Text="Настройки приложения" Style="{StaticResource HeaderTextStyle}"/>

            <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Margin="0,4,0,8">
                <Button Content="Сохранить" Style="{StaticResource InteractiveButtonStyle}"/>
                <Button Content="Отменить"  Style="{StaticResource InteractiveButtonStyle}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,8,0,5" VerticalAlignment="Center">
                <CheckBox x:Name="ToggleButton" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="Включить режим" VerticalAlignment="Center"/>
            </StackPanel>

            <Button Content="Специальная кнопка"
              Style="{StaticResource InteractiveButtonStyle}"
              IsEnabled="{Binding IsChecked, ElementName=ToggleButton}"/>
        </StackPanel>
    </Border>
</Window>
